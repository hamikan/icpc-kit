#!/usr/bin/env bash
set -euo pipefail

if [ -n "${ICPC_KIT:-}" ]; then
  ROOT="$ICPC_KIT"
else
  ROOT="$(CDPATH= cd -P "$(dirname "$0")/.." && pwd)"
fi

python3 "$ROOT/scripts/new_work/new_work.py" "$@"
